import numpy.array as arr
import matplotlib.animation as an
import numpy.random as rand

n_loop = 100         # length of simulation
n_cell = 1           # number of cells at the beginning
divTime = 1          # division time of cells
gridSize = 100       # size of grid side, square root of possible number of cells







cell = arr([round(rand.random() * gridSize)])










